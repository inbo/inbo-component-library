{
  "$schema": "http://json-schema.org/schema",
  "$id": "NgAddSchema",
  "title": "Ng Add Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": ["projectName"]
}
